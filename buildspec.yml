version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 12
  build:
    commands:
      - sam package --template-file template.yaml --s3-bucket cfn-training-takaoukudou-20210411 --output-template-file packaged-template.yml
      - cd ${CODEBUILD_SRC_DIR}
      - cd vue-project
      - npm i -g @mablhq/mabl-cli
      - npm install wait-on
      - npm run serve & wait-on http://localhost:8080
      - mabl auth activate-key b1crrVf7k4LYP80p9ubc1g
      - mabl auth info
      - mabl tests run --id XvqiWzLGhZaxeCi4lzcqxg-j --credentials-id evvlnones0ZDGwgGSKVyBQ-c
artifacts:
  files:
    - packaged-template.yml
  name: packaged-template.yml
